import { CellPhoneDirective } from './cell-phone.directive';

describe('CellPhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new CellPhoneDirective();
    expect(directive).toBeTruthy();
  });
});
